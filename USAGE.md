# Инструкция по использованию программы "Симплекс-метод"

## Описание

Программа позволяет решать задачи линейного программирования симплекс-методом и методом искусственного базиса.

## Запуск программы

```bash
npm install
npm run dev
```

## Основные возможности

### 1. Ввод данных

#### Размерность задачи
- **Переменных**: от 2 до 16
- **Ограничений**: от 2 до 16

#### Целевая функция
Введите коэффициенты целевой функции в формате:
- Целые числа: `1`, `-2`, `5`
- Дроби: `1/2`, `-3/4`, `2/3`
- Десятичные дроби: `0.5`, `-0.75`

Выберите тип оптимизации: `min` или `max`

#### Ограничения
Введите коэффициенты ограничений в каноническом виде (со знаком равенства):
```
a₁₁x₁ + a₁₂x₂ + ... + a₁ₙxₙ = b₁
a₂₁x₁ + a₂₂x₂ + ... + a₂ₙxₙ = b₂
...
```

### 2. Выбор базиса

#### Ручной выбор базиса
Если вы знаете допустимый начальный базис:
1. Снимите галочку "Использовать метод искусственного базиса"
2. Выберите базисные переменные (количество должно равняться количеству ограничений)
3. Убедитесь, что выбранный базис допустимый

#### Метод искусственного базиса
Если начальный базис неизвестен:
1. Установите галочку "Использовать метод искусственного базиса"
2. Программа автоматически найдет начальный допустимый базис

### 3. Режимы решения

#### Автоматический режим
- Нажмите кнопку "Автоматический режим"
- Нажмите "Решить автоматически"
- Программа покажет все шаги решения
- Каждый шаг можно раскрыть в секции "История решения"

#### Пошаговый режим
- Нажмите кнопку "Пошаговый режим"
- Нажмите "Начать пошаговое решение"
- На каждом шаге:
  - Возможные опорные элементы подсвечиваются синим
  - Кликните на нужный элемент для выполнения шага
  - Используйте кнопки "← Предыдущий шаг" и "Следующий шаг →" для навигации
- **Примечание**: пошаговый режим доступен только для задач с заданным базисом

### 4. Работа с файлами

#### Сохранение задачи
1. Нажмите "Сохранить в файл"
2. Файл сохранится в формате JSON

#### Загрузка задачи
1. Нажмите "Загрузить из файла"
2. Выберите ранее сохраненный JSON файл
3. Все параметры задачи будут восстановлены

### 5. Интерпретация результатов

#### Симплекс-таблица
- **Базис** - текущие базисные переменные
- **Столбцы** - небазисные переменные
- **β** - правая часть (свободные члены)
- **f(x)** - строка целевой функции

#### Результаты решения
- **Оптимальное решение найдено** - задача решена, показаны значения переменных
- **Целевая функция не ограничена** - задача не имеет оптимального решения
- **Задача не имеет решения** - система ограничений несовместна

## Примеры задач

### Пример 1: Простая задача
```
f(x) = x₁ + x₂ → min

При ограничениях:
x₁ + x₂ = 4
x₁ - x₂ = 2
xᵢ ≥ 0
```

Ввод:
- Переменных: 2
- Ограничений: 2
- Целевая функция: `1`, `1`
- Ограничения: 
  - Строка 1: `1`, `1`, `4`
  - Строка 2: `1`, `-1`, `2`
- Базис: x₁, x₂ (или использовать искусственный базис)

### Пример 2: Задача с дробями
```
f(x) = 1/2·x₁ + 3/4·x₂ → max

При ограничениях:
2·x₁ + x₂ = 10
x₁ + 2·x₂ = 8
xᵢ ≥ 0
```

Ввод:
- Переменных: 2
- Ограничений: 2
- Целевая функция: `1/2`, `3/4`, тип: `max`
- Ограничения:
  - Строка 1: `2`, `1`, `10`
  - Строка 2: `1`, `2`, `8`

## Справка по ошибкам

### "Необходимо выбрать N базисных переменных"
Количество базисных переменных должно равняться количеству ограничений.

### "Ошибка в формате чисел"
Проверьте правильность ввода дробей. Используйте формат `числитель/знаменатель`, например: `1/2`, `-3/4`.

### "Система вырождена для данного базиса"
Выбранный базис не является допустимым. Попробуйте:
1. Выбрать другие базисные переменные
2. Использовать метод искусственного базиса

### "Пошаговый режим доступен только для задач с заданным допустимым базисом"
Пошаговый режим не поддерживается для метода искусственного базиса. Используйте автоматический режим.

## Контекстная помощь

Наведите курсор на элементы интерфейса для получения дополнительной информации:
- **Подсвеченные ячейки (синие)** - возможные опорные элементы, кликните для выбора
- **Зеленая ячейка** - выбранный опорный элемент на текущем шаге

## Технические требования

- Современный веб-браузер (Chrome, Firefox, Edge)
- JavaScript включен
- Размер экрана: минимум 1024x768 для комфортного просмотра таблиц

## Формат канонической задачи ЛП

Программа работает с задачами в **канонической форме**:

```
f(x) = c₁x₁ + c₂x₂ + ... + cₙxₙ → min (или max)

При ограничениях:
a₁₁x₁ + a₁₂x₂ + ... + a₁ₙxₙ = b₁
a₂₁x₁ + a₂₂x₂ + ... + a₂ₙxₙ = b₂
...
aₘ₁x₁ + aₘ₂x₂ + ... + aₘₙxₙ = bₘ

xᵢ ≥ 0, i = 1..n
```

Если ваша задача содержит неравенства, преобразуйте их в равенства с помощью дополнительных переменных.

## Советы по использованию

1. **Проверяйте ввод**: После ввода данных проверьте правильность коэффициентов
2. **Используйте дроби**: Для точных вычислений используйте дроби вместо десятичных чисел
3. **Сохраняйте задачи**: Сохраняйте сложные задачи в файл для повторного использования
4. **Пошаговый режим**: Используйте для обучения и понимания работы симплекс-метода
5. **Автоматический режим**: Используйте для быстрого получения результата

## Алгоритм симплекс-метода

1. Формирование начальной симплекс-таблицы
2. Проверка оптимальности решения
3. Выбор опорного элемента (разрешающей строки и столбца)
4. Пересчет таблицы методом Жордана-Гаусса
5. Переход к шагу 2

## Метод искусственного базиса (двухфазный метод)

### Фаза I
1. Введение искусственных переменных
2. Решение вспомогательной задачи
3. Проверка допустимости исходной задачи

### Фаза II
1. Удаление искусственных переменных
2. Решение исходной задачи симплекс-методом

## Контакты и поддержка

Для сообщений об ошибках и предложений создайте issue в репозитории проекта.